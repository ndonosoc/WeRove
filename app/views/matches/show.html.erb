<div class="total-match-show-container">

  <div class="aboutcontainer">
    <div class="header">
      <div class="container mx-0 px-0">
        <div class="title mt-2">
        <h1 class="pr-2"><%= @matched_user.first_name %></h1>
        <img src="https://www.countryflags.io/<%= @matched_user.country_flag %>/shiny/32.png" class="mt-1" id="flag">
        </div>
        <div class="location">
        <i class="fas fa-map-marker-alt fa-xs pr-1"></i><p>Local in <strong><%= @geocode.first.city %></strong></p>
        </div>
      </div>
      <% if @matched_user.photo.attached? %>
      <%= cl_image_tag @matched_user.photo.key, id: "avatar", class: "avatar-bordered my-2" %>
      <% else %>
      <img src="https://www.cashforkidsgive.co.uk/static/images/peer-2-peer/no-profile-pic.png" alt="" class="avatar-bordered my-2" id="avatar">
      <% end %>
    </div>
  <p style="padding-top: 8px;"><%= @matched_user.biography %></p>
  </div>

  <div>
    <ul class="nav nav-tabs mt-3" id="myTab" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" id="about-tab" data-toggle="tab" href="#about" role="tab" aria-controls="about" aria-selected="true"></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" id="recommendations-tab" data-toggle="tab" href="#recommendations" role="tab" aria-controls="recommendations" aria-selected="false"></a>
      </li>
    </ul>
  </div>

  <div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="about" role="tabpanel" aria-labelledby="about-tab">

      <%= render 'aboutme', {user: @matched_user, match: @match, age: @age, people_connected: @people_connected} %>

    </div>
    <div class="tab-pane fade" id="recommendations" role="tabpanel" aria-labelledby="recommendations-tab">
      <%= render 'toprecommendations', user: @matched_user %>
    </div>
  </div>

  <div class="floating-chat">
      <%= link_to match_messages_path(@match) do %><i class="fas fa-paper-plane fa-3x" aria-hidden="true"></i> <% end %>
  </div>

</div>
