<!DOCTYPE html>
<html class= "h-100">
  <head>
    <!-- TWITTER METADATA CARD -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="We Rove - Connecting Travelers with Locals everywhere">
    <meta name="twitter:description" content="We Rove is the app which allows you to connect and share experiences with Travelers or Locals all around the world.">
    <meta name="twitter:image:src" content="werovelogo.png">
    <!-- FACEBOOK METADATA CARD -->
    <meta property="og:title" content="We Rove - Connecting Travelers with Locals everywhere">
    <meta property="og:image" content="werovelogo.png">
    <meta property="og:description" content="We Rove is the app which allows you to connect and share experiences with Travelers or Locals all around the world.">
    <meta property="og:site_name" content="We Rove"/>

    <!-- GOOGLE METADATA CARD-->
    <meta name="description" content="We Rove is the app which allows you to connect and share experiences with Travelers or Locals all around the world.">

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="HandheldFriendly" content="true" />
    <title>weRove - Connect with rovers everywhere</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-slider/10.0.0/css/bootstrap-slider.min.css">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable"  content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>
  <body class="main-body h-100 d-flex flex-column">
    <% if notice %>
    <div class="notice_wrapper">
      <p class="notice"><%= notice %></p>
    </div>
    <% elsif alert %>
    <div class="notice_wrapper">
      <p class="alert"><%= alert %></p>
    </div>
    <% end %>

    <% unless current_page?(user_registration_path) %>
    <% if user_signed_in? %>
      <%= render 'layouts/navbar' unless params[:controller] == "messages" && params[:action] == "index" %>
      <% end %>
      <%= render 'shared/flashes' %>
    <% end %>
    <% if user_signed_in? %>
      <div style="<%= params[:controller] == "messages" && params[:action] == "index" ? '' : 'padding-top: 70px;' %>" class="d-flex flex-column h-100">
    <% end %>
        <%= yield %>
    <% if user_signed_in? %>
      </div>
      <% end %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
